<script setup>
import { Link } from '@inertiajs/vue3';
defineProps({ links: Array });
</script>

<template>
  <div v-if="links.length > 3">
    <div class="flex flex-wrap -mb-1">
      <template v-for="link in links">
        <div v-if="link.url === null" v-html="link.label"
          class="mr-1 mb-1 px-4 py-3 text-sm leading-4 text-gray-400 border rounded" />
        <Link v-else :href="link.url" v-html="link.label"
          class="mr-1 mb-1 px-4 py-3 text-sm leading-4 border rounded hover:bg-white hover:text-gray-500 focus:border-indigo-500 focus:text-indigo-500"
          :class="{ 'bg-blue-700 text-white': link.active }" />
      </template>
    </div>
  </div>
</template>